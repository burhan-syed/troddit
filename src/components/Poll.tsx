//TODO -
// /poll routes don't appear to have public json api

import React from "react";

// no oauth api route to vote on polls
const Poll = ({ poll_data }) => {
  return <div>Poll</div>;
};

export default Poll;
